<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owner.spacetraders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Purchase"></activity>
        <activity android:name=".View.Sell_Interface" />
        <activity android:name=".View.Market" />
        <activity android:name=".View.Travel" />
        <activity android:name=".View.Inventory" />
        <activity android:name=".View.Game_Status" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.PlayerCreationActivity" />
        <activity android:name=".View.GameStateActivity" />
    </application>

</manifest>